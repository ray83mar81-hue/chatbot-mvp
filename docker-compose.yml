services:
  chatbot:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    volumes:
      - chatbot-data:/app/backend/data
    restart: unless-stopped

volumes:
  chatbot-data:
